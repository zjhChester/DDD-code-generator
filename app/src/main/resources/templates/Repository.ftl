package com.${company_name}.${project_name}.domain.${capital_upper_table_name?lower_case};

import com.${company_name}.${project_name}.domain.core.Repository;

public interface ${capital_upper_table_name}Repository extends Repository {

}