package com.${company_name}.${project_name}.adapter.inbound.rest.resources.${capital_upper_table_name?lower_case};

import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequiredArgsConstructor
public class ${capital_upper_table_name}Controller {

}